
  # Design Authentication Module (Copy)

  This is a code bundle for Design Authentication Module (Copy). The original project is available at https://www.figma.com/design/8hzkCbM3lwDbu8va3gL7Kt/Design-Authentication-Module--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  